<%= @attraction.name %>

<% if current_user.admin %>
  <%= link_to 'Edit Attraction', edit_attraction_path(@attraction) %>
<% else %>
  <%= form_tag '/ride' do %>
  <%= hidden_field 'ride', 'attraction_id', :value => @attraction.id %>
  <%= submit_tag 'Go on this ride' %>
  <% end %>
<% end %>
